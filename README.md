## Requirements
- Flask
- mysql
- connector-python
- Werkzeug 

## Installation 
1. Clone the repository
2. Install Dependencies pip install -r requirements.txt
3. Run `app.py`

## Database Setup 
1. Create a MySQL database.
2. Import the provided SQL file (if included).
3. Configure your database credentials in `connection.py`
